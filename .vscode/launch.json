{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main nutrition evaluation",
            "type": "debugpy",
            "request": "launch",
            "module": "canasta_inteligente.app.main",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            },
            "console": "integratedTerminal"
        }
    ]
}
